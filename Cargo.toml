[package]
name = "smartloop-cli"
version = "1.0.0"
edition = "2024"

[dependencies]
serde_json = "1"
clap = { version = "4.6.6", features = ["derive"] }
reqwest = { version = "0.12", features = ["blocking", "json"] }
prettytable-rs = "^0.10"